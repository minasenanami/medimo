<div class="form-group">
  <%= search_form_for @q, url: articles_path do |f| %>
    <%= f.search_field :title_or_tags_tag_name_cont, placeholder: "キーワードを入力", required: true, class:"search-bar" %>
    <%= f.submit "検索", class:"search-button" %>
  <% end %>

</div>


<h1>最新の投稿一覧</h1>
<%= render @articles %>
<%= paginate @articles %>
