<% flash.each do |flash_type, msg| %>
  <div class="alert alert-<%= flash_type %>" role="alert">
    <a href="#" class="close text-white" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>
